SauceBot! :D

SauceBot node.js server:

  Build:
  $ coffee --compile --output server/bin/ server/src/

  Running:
  $ cd server/bin
  $ saucebot.js

  Requirements:
    - CoffeeScript
    - Node.js
    + NPM
        - colors
